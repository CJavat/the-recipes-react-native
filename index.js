/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TheRecipesApp from './src/TheRecipesApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TheRecipesApp);
